# Cursor AI Rules для AI Generator Project

## 🔄 АВТОМАТИЧНІ GIT КОМІТИ

**ВАЖЛИВО**: Після кожного успішного оновлення коду (додавання/зміна файлів), автоматично виконуй:

1. ✅ **Додай змінені файли** до staging
2. ✅ **Створи коміт** з описовим повідомленням українською
3. ✅ **Запуш на GitHub** для автоматичного редеплою на Vercel

### Формат комітів

Використовуй формат Conventional Commits:
- `feat:` - нова функціональність
- `fix:` - виправлення помилок
- `refactor:` - рефакторинг
- `style:` - стилі/форматування
- `docs:` - документація
- `chore:` - інші зміни

**Приклад:**
```
feat: Додано посилання на отримання API ключів для нейромереж
fix: Виправлено помилку в адмін панелі
refactor: Оновлено провайдери для використання getApiKey()
```

### Процес

Після завершення завдання користувача:
```bash
git add [змінені_файли]
git commit -m "опис змін"
git push
```

**Не чекай дозволу** - виконуй автоматично, якщо:
- ✅ Код змінено успішно
- ✅ Немає помилок лінтера
- ✅ Тести проходять (якщо є)
- ✅ Користувач попросив оновити сайт/код

**НЕ роби автоматичний коміт**, якщо:
- ❌ Користувач явно просив не комітити
- ❌ Є незавершені зміни
- ❌ Код не працює або має критичні помилки

---

## 📋 ЗАГАЛЬНІ ПРАВИЛА

### Мова
- Всі коментарі та повідомлення - **українською мовою**
- Код та назви змінних - англійською (стандарт індустрії)

### Структура проекту
- Базова директорія: `E:\проекти\ai-generator`
- React/Next.js проект з TypeScript
- Prisma ORM для БД
- Tailwind CSS для стилів

### Безпека
- Ніколи не коміть `.env` файли
- API ключі шифруються перед збереженням
- Перевіряй права доступу перед комітом

### Якість коду
- Завжди перевіряй лінтер перед комітом
- Використовуй TypeScript типи
- Додавай коментарі для складних місць

---

## 🚀 DEPLOYMENT

- Після push на `main` гілку - автоматичний редеплой на Vercel
- Перевіряй статус деплою в Vercel dashboard
- Якщо деплой не спрацював - повідом користувача

---

*Останнє оновлення: 2026-01-15*
